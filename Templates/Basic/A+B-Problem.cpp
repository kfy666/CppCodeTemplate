//A+B Problem
#include<bits/stdc++.h>
using namespace std;
const int N = 1e5 + 5;

int a, b;//使用全局变量, 默认值为0

int main()
{
	scanf("%d%d", &a, &b);//输入a, b
	printf("%d\n", a + b);//输出a + b
	//也可以使用以下两行代码
	//cin >> a >> b;//输入a, b
	//cout << a + b << endl;//输出a + b
	return 0;
}

